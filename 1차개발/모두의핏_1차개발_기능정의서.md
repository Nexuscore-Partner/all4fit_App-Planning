# 모두의 핏 1차 개발 기능 정의서

## 📋 개요
모두의 핏 앱 1차 개발을 위한 상세 기능 정의서

## 🏗️ 시스템 아키텍처

### 플랫폼 구성
- **모바일 앱**: 일반 사용자, 시설 운영자, 지도자
- **웹 시스템**: 관리자 전용

### 기술 스택
- **Frontend**: React Native (모바일), React (웹)
- **Backend**: Node.js + Express
- **Database**: MongoDB
- **Authentication**: JWT + OAuth (카카오)
- **Map API**: Kakao Maps API / Naver Maps API
- **Push Notification**: Firebase Cloud Messaging

## 👥 사용자 역할 및 권한

### 1. 일반 사용자
- **권한**: 시설 조회, 리뷰 작성, 커뮤니티 참여
- **주요 기능**: 시설 검색, 즐겨찾기, 리뷰 작성, 게시판 참여

### 2. 시설 운영자
- **권한**: 일반 사용자 권한 + 시설 정보 관리
- **주요 기능**: 시설 정보 수정, 리뷰 관리, 고객 관리

### 3. 지도자
- **권한**: 일반 사용자 권한 + 문의 답변
- **주요 기능**: 1:1 문의 답변, 프로필 관리

### 4. 관리자
- **권한**: 모든 기능 접근
- **주요 기능**: 사용자 관리, 콘텐츠 관리, 시스템 관리

## 🔐 인증 및 보안

### 인증 방식
1. **통합 로그인**: 이메일/전화번호 + 비밀번호
2. **소셜 로그인**: 카카오 API 연동
3. **비밀번호 찾기**: 이름 + 전화번호 인증
4. **관리자 인증**: 웹 기반 + 2단계 인증

### 보안 정책
- JWT 토큰 기반 인증
- 비밀번호 암호화 (bcrypt)
- API 요청 제한 (Rate Limiting)
- HTTPS 통신 강제

## 📱 모바일 앱 기능

### 1. 인증 시스템

#### 1.1 로그인
```typescript
interface LoginRequest {
  emailOrPhone: string;
  password: string;
}

interface LoginResponse {
  token: string;
  user: User;
  expiresIn: number;
}
```

#### 1.2 회원가입
```typescript
interface SignupRequest {
  userType: 'general' | 'instructor' | 'operator';
  name: string;
  email: string;
  phone: string;
  password: string;
  nickname: string;
  birthDate: string;
  gender: 'male' | 'female';
  location: string;
  agreeTerms: boolean;
  agreePrivacy: boolean;
}
```

### 2. 시설 관리

#### 2.1 시설 검색
```typescript
interface FacilitySearchRequest {
  keyword?: string;
  category?: string;
  location: {
    lat: number;
    lng: number;
    radius: number;
  };
  sortBy: 'distance' | 'rating' | 'favorite';
}

interface Facility {
  id: string;
  name: string;
  address: string;
  category: string;
  operatingHours: string; // 텍스트 형태
  contact: string;
  images: string[];
  rating: number;
  reviewCount: number;
  isFavorite: boolean;
  location: {
    lat: number;
    lng: number;
  };
}
```

#### 2.2 시설 상세
```typescript
interface FacilityDetail extends Facility {
  description: string;
  facilities: string[];
  rules: string;
  reviews: Review[];
  operator: {
    name: string;
    contact: string;
  };
}
```

### 3. 리뷰 시스템

#### 3.1 리뷰 작성
```typescript
interface ReviewRequest {
  facilityId: string;
  rating: number; // 1-5
  content: string;
  images?: string[];
}

interface Review {
  id: string;
  userId: string;
  userName: string;
  facilityId: string;
  rating: number;
  content: string;
  images: string[];
  createdAt: string;
  reply?: string; // 운영자 답글
}
```

### 4. 커뮤니티 시스템

#### 4.1 게시판
```typescript
interface PostRequest {
  title: string;
  content: string;
  images?: string[];
}

interface Post {
  id: string;
  userId: string;
  userName: string;
  title: string;
  content: string;
  images: string[];
  likeCount: number;
  commentCount: number;
  createdAt: string;
  updatedAt: string;
  comments: Comment[];
  isLiked: boolean;
}

interface Comment {
  id: string;
  postId: string;
  userId: string;
  userName: string;
  content: string;
  createdAt: string;
  isLiked: boolean;
  likeCount: number;
}
```

### 5. 즐겨찾기 시스템
```typescript
interface FavoriteRequest {
  facilityId: string;
  action: 'add' | 'remove';
}
```

## 🖥️ 웹 관리 시스템

### 1. 관리자 대시보드
```typescript
interface DashboardData {
  userStats: {
    totalUsers: number;
    newUsers: number;
    activeUsers: number;
  };
  facilityStats: {
    totalFacilities: number;
    newFacilities: number;
    activeFacilities: number;
  };
  reviewStats: {
    totalReviews: number;
    averageRating: number;
    newReviews: number;
  };
  inquiryStats: {
    totalInquiries: number;
    pendingInquiries: number;
    answeredInquiries: number;
  };
}
```

### 2. 사용자 관리
```typescript
interface UserManagement {
  users: User[];
  pagination: Pagination;
  filters: {
    userType?: string;
    status?: string;
    dateRange?: DateRange;
  };
}

interface User {
  id: string;
  email: string;
  phone: string;
  name: string;
  nickname: string;
  userType: 'general' | 'instructor' | 'operator';
  status: 'active' | 'inactive' | 'suspended';
  createdAt: string;
  lastLoginAt: string;
}
```

## 📊 데이터베이스 스키마

### 1. 사용자 스키마
```javascript
const UserSchema = {
  _id: ObjectId,
  email: String,
  phone: String,
  password: String, // 암호화됨
  name: String,
  nickname: String,
  birthDate: Date,
  gender: String,
  location: String,
  userType: String, // 'general', 'instructor', 'operator'
  status: String, // 'active', 'inactive', 'suspended'
  profileImage: String,
  createdAt: Date,
  updatedAt: Date,
  lastLoginAt: Date
};
```

### 2. 시설 스키마
```javascript
const FacilitySchema = {
  _id: ObjectId,
  name: String,
  address: String,
  category: String,
  subCategory: String,
  operatingHours: String, // 텍스트 형태
  contact: String,
  description: String,
  images: [String],
  facilities: [String], // 편의시설
  rules: String,
  location: {
    lat: Number,
    lng: Number
  },
  operatorId: ObjectId,
  status: String, // 'active', 'inactive', 'closed'
  rating: Number,
  reviewCount: Number,
  createdAt: Date,
  updatedAt: Date
};
```

### 3. 리뷰 스키마
```javascript
const ReviewSchema = {
  _id: ObjectId,
  userId: ObjectId,
  facilityId: ObjectId,
  rating: Number,
  content: String,
  images: [String],
  reply: String, // 운영자 답글
  replyAt: Date,
  status: String, // 'active', 'reported', 'deleted'
  createdAt: Date,
  updatedAt: Date
};
```

### 4. 게시글 스키마
```javascript
const PostSchema = {
  _id: ObjectId,
  userId: ObjectId,
  title: String,
  content: String,
  images: [String],
  likeCount: Number,
  commentCount: Number,
  status: String, // 'active', 'reported', 'deleted'
  createdAt: Date,
  updatedAt: Date
};
```

### 5. 댓글 스키마
```javascript
const CommentSchema = {
  _id: ObjectId,
  postId: ObjectId,
  userId: ObjectId,
  content: String,
  likeCount: Number,
  status: String, // 'active', 'reported', 'deleted'
  createdAt: Date,
  updatedAt: Date
};
```

## 🔄 비즈니스 로직

### 1. 시설 검색 로직
```typescript
class FacilityService {
  async searchFacilities(searchRequest: FacilitySearchRequest): Promise<Facility[]> {
    // 1. 키워드 검색 (시설명, 주소, 카테고리)
    // 2. 위치 기반 필터링 (반경 내)
    // 3. 카테고리 필터링
    // 4. 정렬 (거리, 평점, 즐겨찾기)
    // 5. 페이지네이션
  }
}
```

### 2. 리뷰 작성 로직
```typescript
class ReviewService {
  async createReview(reviewRequest: ReviewRequest, userId: string): Promise<Review> {
    // 1. 사용자 권한 확인
    // 2. 중복 리뷰 확인
    // 3. 리뷰 생성
    // 4. 시설 평점 업데이트
    // 5. 운영자 알림 발송
  }
}
```

### 3. 게시글 작성 로직
```typescript
class PostService {
  async createPost(postRequest: PostRequest, userId: string): Promise<Post> {
    // 1. 사용자 권한 확인
    // 2. 콘텐츠 검증 (부적절한 내용 필터링)
    // 3. 게시글 생성
    // 4. 이미지 업로드 처리
  }
}
```

## 📱 UI/UX 가이드라인

### 1. 디자인 원칙
- **일관성**: 전체 앱에서 일관된 디자인 언어
- **접근성**: 모든 사용자가 쉽게 사용 가능
- **반응성**: 다양한 화면 크기에 대응
- **직관성**: 사용자가 쉽게 이해할 수 있는 인터페이스

### 2. 네비게이션 구조
```
하단 탭바 (5개)
├── 홈 (🏠)
├── 찜 (❤️) - 2차 개발
├── 지도 (🗺️)
├── 커뮤니티 (💬)
└── 마이페이지 (👤)
```

### 3. 상태 관리
- **인증 상태**: 로그인/로그아웃 상태
- **네비게이션 상태**: 현재 탭, 화면 스택
- **데이터 상태**: 캐시된 데이터 관리
- **UI 상태**: 로딩, 에러 상태

## 🔔 알림 시스템

### 1. 푸시 알림
```typescript
interface PushNotification {
  title: string;
  body: string;
  data?: any;
  userId?: string;
  userType?: string;
}

// 알림 타입
enum NotificationType {
  NEW_REVIEW = 'new_review',
  NEW_COMMENT = 'new_comment',
  NEW_POST = 'new_post',
  SYSTEM_ANNOUNCEMENT = 'system_announcement'
}
```

### 2. 알림 설정
- 사용자별 알림 on/off 설정
- 알림 타입별 개별 설정
- 알림 시간 설정 (방해금지 시간)

## 🚀 성능 최적화

### 1. 이미지 최적화
- 이미지 압축 및 리사이징
- 지연 로딩 (Lazy Loading)
- CDN 사용

### 2. API 최적화
- 응답 데이터 압축
- 캐싱 전략
- 페이지네이션

### 3. 데이터베이스 최적화
- 인덱스 최적화
- 쿼리 최적화
- 연결 풀 관리

## 🧪 테스트 전략

### 1. 단위 테스트
- 비즈니스 로직 테스트
- 유틸리티 함수 테스트

### 2. 통합 테스트
- API 엔드포인트 테스트
- 데이터베이스 연동 테스트

### 3. E2E 테스트
- 사용자 시나리오 테스트
- 크로스 플랫폼 테스트

## 📈 모니터링 및 분석

### 1. 성능 모니터링
- API 응답 시간
- 데이터베이스 성능
- 에러 발생률

### 2. 사용자 분석
- 사용자 행동 분석
- 기능 사용률 분석
- 이탈률 분석

### 3. 비즈니스 지표
- 사용자 증가율
- 시설 등록 증가율
- 리뷰 작성률

## 🔧 개발 환경 설정

### 1. 개발 도구
- **IDE**: VS Code
- **버전 관리**: Git
- **패키지 관리**: npm/yarn
- **빌드 도구**: Metro (React Native)

### 2. 개발 프로세스
1. 기능 개발
2. 코드 리뷰
3. 테스트
4. 스테이징 배포
5. 프로덕션 배포

### 3. 배포 전략
- **모바일 앱**: App Store, Google Play
- **웹 관리자**: 클라우드 서버 배포
- **API**: RESTful API 서버

이 기능 정의서는 모두의 핏 앱의 1차 개발을 위한 상세한 기술 명세를 제공하며, 개발팀이 일관되고 효율적으로 개발을 진행할 수 있도록 가이드라인을 제시합니다.
